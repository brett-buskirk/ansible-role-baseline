# ansible-role-baseline

Ansible role that hardens a fresh Ubuntu host — disables root SSH login, sets up a UFW firewall + fail2ban, and installs Docker + Tailscale — on top of ansible-role-secure_user.
