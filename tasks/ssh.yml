# ref: tasks/ssh.yml

---
- name: Disable root SSH login
  lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '^#?PermitRootLogin'
    line: 'PermitRootLogin no'
    # Never write a config that would break sshd — validate before saving.
    validate: 'sshd -t -f %s'
  notify: Restart SSH
